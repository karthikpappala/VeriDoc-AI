# VeriDoc AI — requirements.txt
# Install: pip install -r requirements.txt

# ── PDF parsing ─────────────────────────────────────────────
PyMuPDF==1.24.5          # fitz — fast PDF text extraction

# ── Chunking & RAG framework ─────────────────────────────────
langchain==0.2.6
langchain-community==0.2.6
langchain-core==0.2.11

# ── Embeddings ───────────────────────────────────────────────
sentence-transformers==3.0.1

# ── Vector store ─────────────────────────────────────────────
faiss-cpu==1.8.0

# ── LLM inference ────────────────────────────────────────────
# Option A: local (needs GPU/decent CPU)
# torch==2.3.1
# transformers==4.42.4
# accelerate==0.32.0
# bitsandbytes==0.43.1

# Option B: API (recommended for Mac without GPU) — uncomment ONE:
groq==0.9.0              # Groq API — free tier, very fast
# openai==1.35.13        # OpenAI API fallback

# ── Evaluation ───────────────────────────────────────────────
ragas==0.1.14
datasets==2.20.0

# ── Language detection ───────────────────────────────────────
langdetect==1.0.9

# ── Multilingual translation (Day 11-12) ─────────────────────
# transformers already included above
# sentencepiece==0.2.0   # needed by NLLB-200

# ── API server ───────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9  # for file uploads
pydantic==2.8.2

# ── Frontend (Day 13+) ───────────────────────────────────────
streamlit==1.36.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.27.0
numpy==1.26.4
